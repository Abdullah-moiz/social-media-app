/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['png.pngtree.com' , 'images.pexels.com' , 'ages.pexels.com'],
  },
}

module.exports = nextConfig
